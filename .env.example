# 複製這個檔案為 .env，填入你的實際 key
# ⚠️  .env 已被 .gitignore 排除，不會上傳到 Git
# ⚠️  部署到 Vercel 時，需到 Vercel Dashboard > Settings > Environment Variables 手動設定

# Google Maps API Key（後端 Proxy 專用，前端看不到）
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
